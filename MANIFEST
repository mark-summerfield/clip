# TODO FIX BUG
# get rid of added; instead have optionState
#   NotGiven // default
#   Given   // -v
#   HadValue // -v1
# and add: Check() error
#
# func newXXXOption(...) {
#   ...
#   option.state = NotGiven
#   return option
# }
#
# func (me XXXOption) setGiven() {
#   me.state = Given
# }
# 
# func (me XXXOption) addValue(...) {
#   ...
#   me.state = HadValue
# }
# 
# func (me XXXOption) Check(atEnd bool) error { // Int Real Str version
#   if atEnd {
#	if me.state == Given && !m.allowImplicit {
#	    return fmt.Errorf(...) # option without value
#	}
#   }
#   return nil
# }
#
#
# TODO
# curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin v1.50.1

# TODO Additional ValueTypes:
#   - Ints
#   - Reals
#   (both with ValueCount OneOrMore)
#   - commensurate tests
# TODO Add tests for all ValueCount's for Positionals
# TODO document all option forms & the corresponding code & make sure
#      all doc egs have corresponding tests
# TODO add tests for every error code
# TODO add tests for every panic code
# TODO add more tests for subcommands

garg.go
token.go
option.go
subcommand.go
util.go
consts.go

garg_test.go

st.sh

# vim: syn=yaml
